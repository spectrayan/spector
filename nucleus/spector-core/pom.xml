<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.spectrayan</groupId>
        <artifactId>spector</artifactId>
        <version>${revision}</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>

    <artifactId>spector-core</artifactId>
    <name>Spector Core</name>
    <description>SIMD-accelerated math kernels and similarity functions via Java Vector API.</description>

    <properties>
        <spector.module.name>com.spectrayan.spector.core</spector.module.name>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.spectrayan</groupId>
            <artifactId>spector-commons</artifactId>
        </dependency>

        <!-- ── Test Dependencies ── -->
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>net.jqwik</groupId>
            <artifactId>jqwik</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <execution>
                        <id>enforce-banned-dependencies</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <rules>
                        <bannedDependencies>
                            <!--
                              ADR-0033 Principle 5. searchTransitive is deliberately false: this rule
                              guards spector-core's DIRECTLY declared dependencies. spector-commons
                              legitimately pulls a large transitive tree (Quartz, Tika/PDFBox,
                              Handlebars, Caffeine), so a transitive scan would fail the build without
                              telling us anything about core's own coupling. The actual import-level
                              boundary — which is what Principle 1 really constrains — is enforced by
                              CoreBoundaryRulesTest (ArchUnit). Do not flip this to true; tighten the
                              ArchUnit rule instead.
                            -->
                            <searchTransitive>false</searchTransitive>
                            <excludes>
                                <exclude>*</exclude>
                            </excludes>
                            <includes>
                                <include>com.spectrayan:spector-commons</include>
                                <include>org.slf4j:slf4j-api</include>
                                <include>*:*:*:*:test</include>
                            </includes>
                            <message>ADR-0033 Principle 1 violation: spector-core must only depend on spector-commons and slf4j-api.</message>
                        </bannedDependencies>
                    </rules>
                    <fail>true</fail>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
