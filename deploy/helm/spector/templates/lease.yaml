apiVersion: coordination.k8s.io/v1
kind: Lease
metadata:
  name: spector-coordinator-{{ include "spector.cellId" . }}
  labels:
    {{- include "spector.labels" . | nindent 4 }}
    app.kubernetes.io/component: coordinator
spec:
  leaseDurationSeconds: {{ .Values.coordinator.leaseDurationSeconds | default 15 }}
  renewTime: null
  acquireTime: null
